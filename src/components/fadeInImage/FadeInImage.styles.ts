import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    marginBottom: 20,
    width: '100%',
    height: 400,
    borderRadius: 10,
  },
  image: {
    width: '100%',
    height: '100%',
    borderRadius: 10,
  },
});
